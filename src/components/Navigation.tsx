import React from 'react';

interface NavigationProps {
  setCalLink: (calLink: string) => void;
}

const Navigation: React.FC<NavigationProps> = ({ setCalLink }) => {
  // Navigation is integrated into Hero component for GHL design
  return null;
};

export default Navigation;